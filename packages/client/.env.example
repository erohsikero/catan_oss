# Only needed when the client is hosted separately from the game server
# (for example a static build on Pages talking to a server on Fly).
# Leave unset when one Node process serves both.
# VITE_SERVER_URL=https://hexhaven.fly.dev
